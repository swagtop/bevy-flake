name: Automatic lockfile update.
on:
  schedule:
    - cron: '39 0 * * 6'
  workflow_dispatch:

jobs:
  update-inputs:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
      - uses: DeterminateSystems/nix-installer-action@main
      - uses: DeterminateSystems/magic-nix-cache-action@main

      - name: Update flake lockfile
        run: nix flake update
      
      - name: Clone bevy repo
        run: git clone https://github.com/bevyengine/bevy.git bevy -b release-0.18.1

      - name: Pull 'fenix' template, follow newly updated lockfile from bevy-flake
        working-directory: bevy
        run: |
          nix flake init --template ..#fenix
          nix flake lock ../.
          nix flake lock --override-input bevy-flake git+file:../. --allow-dirty-locks

      # Build each target, cleaning each afterwards to not exceed maximum storage usage for runner.

      - name: Build Breakout for aarch64-pc-windows-msvc
        working-directory: bevy
        run: |
          nix run .#rust-toolchain build -- --locked --example breakout --target aarch64-pc-windows-msvc
          nix run .#rust-toolchain clean -- --target aarch64-pc-windows-msvc

      - name: Build Breakout for x86_64-pc-windows-msvc
        working-directory: bevy
        run: |
          nix run .#rust-toolchain build -- --locked --example breakout --target x86_64-pc-windows-msvc
          nix run .#rust-toolchain clean -- --target x86_64-pc-windows-msvc

      - name: Build Breakout for aarch64-unknown-linux-gnu
        working-directory: bevy
        run: |
          nix run .#rust-toolchain build -- --locked --example breakout --target aarch64-unknown-linux-gnu
          nix run .#rust-toolchain clean -- --target aarch64-unknown-linux-gnu

      - name: Build Breakout for x86_64-unknown-linux-gnu
        working-directory: bevy
        run: |
          nix run .#rust-toolchain build -- --locked --example breakout --target x86_64-unknown-linux-gnu
          nix run .#rust-toolchain clean -- --target x86_64-unknown-linux-gnu

      - name: Build Breakout for aarch64-apple-darwin
        working-directory: bevy
        run: |
          nix run .#rust-toolchain build -- --locked --example breakout --target aarch64-apple-darwin
          nix run .#rust-toolchain clean -- --target aarch64-apple-darwin

      - name: Build Breakout for x86_64-apple-darwin
        working-directory: bevy
        run: |
          nix run .#rust-toolchain build -- --locked --example breakout --target x86_64-apple-darwin
          nix run .#rust-toolchain clean -- --target x86_64-apple-darwin

      - name: Build Breakout for wasm32-unknown-unknown
        working-directory: bevy
        run: |
          nix run .#rust-toolchain build -- --locked --example breakout --target wasm32-unknown-unknown 
          nix run .#rust-toolchain clean -- --target wasm32-unknown-unknown

      # If every target builds successfully, update is good and we can push.
        
      - name: Push update lockfile
        run: |
          git config --global user.name 'github-actions'
          git config --global user.email 'github-actions@users.noreply.github.com'
          git add ./flake.lock
          git commit -m "Automatic lockfile update." | true
          git push origin main
